extends layout

block content
  h1= title
  p Welcome to My world Varsha...! I love dancing,singing, playing  badminton, spending time with pets.
  p I have a pet cat named Shero
  
     button(type="button" , class="btn btn-secondary", onClick="reduce()") PRESS ME

     div(id="reduce" ) REDUCE THE VALUE
     a(href="queen") Link
     script.
      let count =20;
      let change = -1;
      function reduce() {
        count = count+change;
        change = change-1;
        document.getElementById("reduce").innerHTML = "Count is " +count+ "Change is" +change;
      }